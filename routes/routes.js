//GLOBAL
const HOME = "/"
const CHAT = "/chat"
const LOGIN = "/login"
const JOIN = "/join"
const LOGOUT = "/logout"
const CART = "/cart"

// admin

const ADMIN = "/admin"
const PRODUCTS = "/products"

const routes = {
    home: HOME,
    admin: ADMIN,
    products: PRODUCTS,
    chat: CHAT,
    login: LOGIN,
    logout: LOGOUT,
    join: JOIN,
    cart: CART
}

export default routes;